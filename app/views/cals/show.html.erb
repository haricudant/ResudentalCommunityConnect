<% content_for(:page_header) {"Monthly expenses including your Rent & selected amenities is!"} %>
<div class="jumbotron  jumbotron-fluid container textcenter">

<p>
  Welcome<strong>  <%= @cal.name %></string>
</p>

<p>
  Total Predicted cost including Rent/EMI is<strong> <%= @cal.rent %>  Euros</strong>
 
</p>



<h1></h1>

<div id="piechart"></div>

<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>

<script type="text/javascript">
// Load google charts
google.charts.load('current', {'packages':['corechart']});
google.charts.setOnLoadCallback(drawChart);

// Draw the chart and set the chart values
function drawChart() {
  var data = google.visualization.arrayToDataTable([
  ['Task', 'Hours per Day'],
  ['Rent', <%= @cal.rent %>],
  ['Sport',  %>],
  ['Laundry', ],
  ['Miscellaneous'],
  ['Pet', ]
  
   
    
]);

  // Optional; add a title and set the width and height of the chart
  var options = {'title':'My Average Day', 'width':550, 'height':400};

  // Display the chart inside the <div> element with id="piechart"
  var chart = new google.visualization.PieChart(document.getElementById('piechart'));
  chart.draw(data, options);
}
</script>

</body>
</html>

</div>

<%= link_to 'Edit', edit_cal_path(@cal) %> |
<%= link_to 'Back', cals_path %>

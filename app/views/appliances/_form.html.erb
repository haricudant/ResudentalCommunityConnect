<div class = "jumbotron">
<%= form_with(model: appliance, local: true) do |form| %>
  <% if appliance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appliance.errors.count, "error") %> prohibited this appliance from being saved:</h2>

      <ul>
      <% appliance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Appliance_name %>
    <%= form.text_field :Appliance_name %>
  </div>

  <div class="field">
    <%= form.label :Appliance_watt %>
    <%= form.number_field :Appliance_watt %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>